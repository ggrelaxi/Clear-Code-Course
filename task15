1) // Форматирование номера к виду +7 999 999-99-99

export const fixPhoneNumberView = (number) => {...

2) // Форматирование номера телефона к виду 79********

export const getFormatPhoneToForis = (number) => {...

3) // Возврат текстового значения по выбранному типу клиента

export const getClientTypeView = (value) => {...

4) // Форматирование чисел в вид с иконкой российского рубля

export const getRoubleFormat = (value) => {...

5)  // TODO: Так как портируемых номеров нет, и не понятно особо,
    //  что именно должно с бэка приходить.
    //  Как только их выдадут, проверить еще раз и добавить
    
6)  // TODO Если есть более гуманный вариант сбрасывать значения в инпутах, при переключении табов (радиокнопок), буду раз совету

7) // Данный метод был отключен, т.к. на бекенде не реализована обработчик

8) // Данный код проверяет наличие любого варианта нужного класса в дереве элементов.
